{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "test": "npx tsx test-suite.ts",
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute safetrack-db --local --file=./schema.sql && wrangler d1 execute safetrack-db --local --file=./seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-validator": "^0.9.1",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.134.0"
  }
}
